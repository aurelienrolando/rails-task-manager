<h1>My TASKS</h1>

<%= link_to "Add a new task", tasks_new_path %>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %></li>
  <li><%= link_to "delete task", task_path(task.id), method: :delete,
   data: { confirm: "Are you sure?" } %></li>
  <% end %>
</ul>
